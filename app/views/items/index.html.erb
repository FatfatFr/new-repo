<h1>All the items</h1>

<div class="allcards">
  <% @items.each do |item| %>

  <div class="card" style="width: 18rem;">

<%= cl_image_tag("xacrvk9drxisoggk2dca",
      width: 400, height: 300, crop: :fill) %>
  <%# <%= image_tag "image.jpg", class: "card-img-top" %>
    <div class="card-body">
      <<%= item.name %>
      <p class="card-text"><%= item.description %></p>
    </div>
  </div>

  <% end %>
</div>
